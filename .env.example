# Copy to .env and fill in. This file is committed; .env is not.
ANTHROPIC_API_KEY=
# Port the backend listens on, e.g. 3000
PORT=
# Password for the staff order screen at /staff.html. The username is "staff".
# Without it the staff screen and the order endpoints are closed, not open.
STAFF_PASSWORD=
